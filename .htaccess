# ======================================================
# 🔒 Laravel Root Security (.htaccess)
# ======================================================
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests to the public folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# ======================================================
# 🚫 Protect Sensitive Files & Folders
# ======================================================
<FilesMatch "(^\.|composer\.json|composer\.lock|artisan|server\.php|.env|.env.*|.git|.htaccess|vendor|node_modules)">
    Order allow,deny
    Deny from all
</FilesMatch>

# ======================================================
# 🧱 Prevent Access to Config, Logs, and Cache
# ======================================================
<DirectoryMatch "(app|bootstrap|config|database|resources|routes|tests|vendor)">
    Order allow,deny
    Deny from all
</DirectoryMatch>

# ✅ Allow public storage (used for uploads)
<Directory "public/storage">
    Allow from all
    Options Indexes FollowSymLinks
    Require all granted
</Directory>

# ======================================================
# 🧯 Disable PHP Execution in sensitive folders
# ======================================================
<Directory "storage">
    php_flag engine off
</Directory>
<Directory "bootstrap">
    php_flag engine off
</Directory>
<Directory "vendor">
    php_flag engine off
</Directory>

# ======================================================
# 🧰 Security Headers
# ======================================================
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header unset X-Powered-By
</IfModule>

# ======================================================
# 🚫 Disable Directory Listing
# ======================================================
Options -Indexes
